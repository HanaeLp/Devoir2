#include <stdio.h>
#include <stdlib.h>

int main()
{
     int A,i;

        for(i=1; i<=2; i++){
            printf("entrer les moyennes de classe %d :\n", i);
            scanf("%d", &A);
        }
}
